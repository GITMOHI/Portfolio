# Nodemailer (server only — add the same keys in Vercel → Settings → Environment Variables).
# Free Gmail: enable 2FA → Google Account → App passwords → use as SMTP_PASS.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
CONTACT_TO_EMAIL=
